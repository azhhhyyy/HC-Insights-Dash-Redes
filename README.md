
  # Build dashboard pages

  This is a code bundle for Build dashboard pages. The original project is available at https://www.figma.com/design/Sd4OjIyZqPm3p8d3vKQSpL/Build-dashboard-pages.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  